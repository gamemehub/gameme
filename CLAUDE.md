## AI実装時の基礎チェック（3つの問い）

AIに実装を任せる前・バグ調査時に、以下を意識する。
指示精度が上がり、フラグ消失・セーブ破損・違和感イベント未発火を防ぎやすくなる。

### 1. 扱うデータは何か（What）
- このフラグ／進行値は何を表すか。
- それを消す・変えると、何が連鎖して壊れるか。
  例：探索完了フラグを消すと、違和感イベントが発火しなくなる等。

### 2. その値はいつ変わり、誰のものか（When / 静と動）
- 「静」＝シーン構造・配置など固定の部分。
- 「動（state）」＝進行状況・記憶ログ・編集状態など状況で変わる部分。
- その値は どこに・いつまで残るか を必ず区別する。
  - 実行中だけ保持（メモリ上のstate）→ シーン遷移／再起動で消える。
  - セーブデータに永続化 → 再起動後も残る。
- 「リロードしたら進行が消えた」「途中状態が二重に残った」はこの線引きミスが原因。
- セーブデータ構造の変更は 高リスク。勝手に変えず、必要時は確認を取る。

### 3. どんな部品・外部サービス（API）を使うか（Where）
- ゼロから作らず、まず Unity 既存コンポーネント／導入済みアセットで足りないか確認する。
- 既存の流れ・命名に合わせて最小変更で実装する。
