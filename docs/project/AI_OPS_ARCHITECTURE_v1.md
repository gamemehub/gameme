# RewriteMemory AI運用アーキテクチャ v1.0

状態: v1.0 / 標準仕様（今後のAI基盤の基準）
作成: 2026-07-02 / 為替 1USD≈150円（要再確認）
関連: DIRECTION_2026-07 / PHASE1_AUTONOMY / AUTONOMY_MACMINI / COST_OPERATIONS_2026-07 / tools/autonomy/
決定: エンジン=Godot / **AI運用=C案(ハイブリッド)を採用**（A不採用・B高コストリスク）。

---

## 1. 全体構成図

```
                    ┌──────────────────────────────────────────────┐
   人間(Chairman)   │  最終承認 / 気持ちよさ判定 / 資金・公開・応募判断 │
        ▲          └───────────────▲──────────────────────────────┘
        │ URLレビュー・番号回答           │ 承認/差戻し
        │                              │
   ┌────┴───────┐   方針/仕様/レビュー基準   ┌┴─────────────┐
   │  GPT       │◀───────────────────────▶│  GitHub      │  ← AI連携のハブ
   │ PM/アーキ/  │   Issue作成・PRレビュー    │ Issue/PR/CI  │
   │ QA/リスク   │                          │ /Actions     │
   └────┬───────┘                          └──▲────┬──────┘
        │ 仕様・優先順位                        │PR  │Ready Issue / CI結果
        ▼                                     │    ▼
   ┌─────────────────── Mac mini (24h常駐 / launchd) ───────────────────┐
   │  ┌───────────┐   ┌───────────┐   ┌───────────┐   ┌─────────────┐ │
   │  │Dispatcher │──▶│ Claude    │   │ Codex     │   │ Supervisor  │ │
   │  │(agent-    │   │ (主実装+  │   │(副実装:   │   │(監視エージェ  │ │
   │  │ runner)   │   │ CI/検証/   │   │ 局所量産) │   │ ント:再起動/  │ │
   │  └───────────┘   │ docs)     │   └───────────┘   │ コスト/CI/暴走)│ │
   │                  └─────┬─────┘                   └─────────────┘ │
   │  セルフホストRunner(任意) │ push                                    │
   └────────────────────────┼───────────────────────────────────────┘
                            ▼
        Anthropic API(Sonnet主力/難所Opus/雑務Haiku) ＋ OpenAI API(GPT/Codex)
```
- **API従量が自律ループの本体**（ToS適合・週次上限なし）。**Maxサブスクは人間の対話開発・軽作業**に限定。
- コード/成果物の一次的真実は **GitHub**。Mac miniローカルはワークスペース（復旧容易化）。

---

## 2. 責務(Role)定義と担当（**担当AIは差替可能**）
**設計原則：製品名でなく"責務(Role)"を固定し、担当AIを交換可能にする。** これによりベンダー非依存となり、将来のGPT-6 / Claude 6 / Codex Next / 他AIへ、担当表を差し替えるだけで移行できる。

### 2.1 責務(Role)
| Role | 内容（何をする） | 位置づけ |
|---|---|---|
| **PM** | ロードマップ・優先順位・Issue作成・進行管理・リスク管理 | 上流(非同期) |
| **Architect Review** | 設計整合・正本整合・invariant・技術リスクの事前レビュー | 節目(非同期) |
| **Implementation** | 実装・commit・push。統括担当は repo/CI/検証/docs を一気通貫 | 実行 |
| **Self Review** | 実装者自身の自己点検（仕様整合・参照切れ・未使用コード・命名・コメント・テスト） | 実装直後(必須) |
| **Cross AI Review** | **実装者以外**のAIによる独立レビュー（別モデル/新規コンテキスト） | 毎PR(非ブロッキング) |
| **QA** | 勝利条件・体験基準に沿った検証 | 節目 |
| **Human Approval** | **ゲーム体験・演出・感情・世界観・UX** の最終判断／承認 | 確認レーンのみ |

### 2.2 現在の担当（**差替可能マッピング**・2026-07時点）
| Role | 現担当 | 将来の差替候補 |
|---|---|---|
| PM | GPT | GPT-6 / Claude / 他 |
| Architect Review | GPT（＋Claudeが地上検証で双方向） | 任意の上位AI |
| Implementation（統括） | **Claude Code**（repo/CI/検証/docs） | Claude / Codex / 他 |
| Implementation（副・局所量産） | Codex | 任意 |
| Self Review | 実装した本人AI | — |
| Cross AI Review | **実装者以外**：GPT-API / 2nd Claude(新規context) / Codex | 追加AI |
| QA | GPT | 任意 |
| Human Approval | 人間(Miya) | 人間固定 |

**原則**
- **責務中心**：AIを固定せず責務を固定。担当は上表で差し替えるだけ＝特定ベンダーに依存しない。
- **「実装したAIはCross Reviewしない」**（本人以外が必ず見る）。
- 判断=PM/Architect/QA/人間、実装=Implementation、承認=Human Approval。迷い・仕様矛盾・大規模改修・本番影響 → **停止して報告**（PROJECT_OPERATING_SYSTEM §14）。

---

## 3. 採用構成（C案・確定）
- **対話/軽作業＝Max 20x（$200/月・定額）**：人間＋Claude Codeの対話開発、docs整備。
- **無人ループ＝API従量**：Sonnet 5主力＋難所のみOpus 4.8＋雑務Haiku 4.5、**キャッシュ徹底＋Batch活用**。
- **QA/レビュー＝GPT/Codex API**（役割特化）。
- **予算ガード＝Supervisorの日次コスト上限**（暴走防止）。
- A（Maxのみ）不採用＝週次上限で自律が途切れ「止まらない基盤」にならない。B（全面API・青天井）は高コストリスクのため予算上限必須。

---

## 4. コスト試算（前提を明示）

### 4.1 標準運用シナリオ（前提・可変）
| 前提 | 値 |
|---|---|
| 常駐 | 24h（launchd）。ただし"生成中"ではなくイベント/スケジュール駆動 |
| 実タスク | 1日 8〜15件（Ready Issue→実装→PR） |
| 1タスクのmodelターン | 約10〜20 |
| **1日の合計model-turn** | **≈150** |
| 1ターンのトークン | 入力60k（キャッシュ率75%→fresh 15k/cache 45k）＋出力6k |
| モデル比率 | **Sonnet 70% / Opus 15% / Haiku 15%** |
| Batch適用 | 非対話処理の約30%（レビュー/検証） |

### 4.2 1ターン単価（上記トークン・導入価格）
| モデル | fresh入力 | cache読取 | 出力 | 合計/ターン |
|---|---|---|---|---|
| Sonnet 5 | 15k×$2 | 45k×$0.2 | 6k×$10 | **≈$0.099** |
| Opus 4.8 | 15k×$5 | 45k×$0.5 | 6k×$25 | **≈$0.248** |
| Haiku 4.5 | 15k×$1 | 45k×$0.1 | 6k×$5 | **≈$0.050** |

- 加重平均（0.7/0.15/0.15）≈ **$0.114/ターン** → 150ターン/日 ≈ **$17/日**、Batch分で ≈ **$15.5/日**。
- **API月額 ≈ $465（約7.0万円）**。
- 合算：Max $200 ＋ API $465 ＋ GPT/Codexレビュー ~$50 ＋ 電気 ~¥500 ＋ バックアップ按分 ≈ **月 $715〜900 ＝ 約10.7〜13万円**（C案レンジと整合）。

### 4.3 感度（何が効くか）
- **Opus比率**：15%→40%で API月額が約1.6倍。難所限定を厳守。
- **ターン数/日**：150→300で API約2倍。日次上限で制御。
- **キャッシュ率**：75%→50%で入力コスト増。安定プレフィックス設計で維持。
- **Sonnet標準価格移行(9/1)**：$2/$10→$3/$15でSonnet分1.5倍。
→ **必ず自環境で `count_tokens` 実測して4.1の係数を再校正**（要再確認）。

---

## 5. 運用フロー
```
[PM] Ready Issue作成(仕様/invariant/完了条件) → ラベル ai-ready
        ▼
[Architect Review] 設計/正本/invariant整合を事前確認(節目・非同期)
        ▼ (Mac mini launchd, 300s間隔)
Dispatcher: ai-ready を1件取得 → claude/ ブランチ作成 → in-progress
        ▼
[Implementation] 統括AI(現Claude)/副(現Codex): 実装 → commit
        ▼
[Self Review] 実装者本人: 仕様整合/参照切れ/未使用/命名/コメント/テスト → push
        ▼
auto-pr: PR作成   [L1 CI] godot-check: 必須ゲート(green)
        ▼
[Cross AI Review] 実装者以外(別モデル/新規context・非ブロッキング): PRコメント
        ▼
安全レーン=自動マージ / 体験系=[Human Approval] URL承認(体験/演出/UXのみ)
        ▼
Merge → Issue close / Supervisor監視 / Daily Report
```

## 5.5 レビュー体制（多層・相互・非ブロッキング）
**大原則：GPT/人間を"毎PRの同期ゲート"にしない**（それが「半自律」ボトルネックの正体）。GPTは上流(Issue/仕様)と節目に非同期で入り、毎PRの品質は自動レビューで担保する。

| 層 | Role | 担当（差替可） | 毎PR止める |
|---|---|---|---|
| **L0 Self Review** | Self Review | 実装者本人AI | ✕（必須自己点検） |
| **L1 CI(godot-check)** | — | 自動 | ✅ 必須ゲート |
| **L2 Cross AI Review** | Cross AI Review | **実装者以外**（GPT-API / 2nd Claude(新規context) / Codex / 追加AI） | ✕ 非ブロッキング（PRコメント） |
| **L3 Architect/設計相互(双方向)** | Architect Review | GPT↔Claude（現担当） | ✕ 非同期 |
| **L4 Human Approval** | Human Approval | Miya（**体験/演出/感情/世界観/UXのみ**） | ✅ 体験系のみ |

- **Self Review(L0)標準化**：実装者本人が push 前に必ず自己点検 — 仕様整合／参照切れ0／未使用コード除去／命名／コメント／テスト。
- **Cross AI Review(L2)**：実装は必ず"作った本人以外"が見る。**別モデル/新規コンテキスト**でレビュー＝多様な失敗モードを捕捉。担当は GPT-API / 2nd Claude / Codex / 追加AI から差替可能。
- **双方向**：GPT→Claude（設計/優先順位/リスク）に加え、**Claude→GPT**（Claudeはrepo実体を見られるので、**GPTの仕様が実装/参照/invariantと矛盾していないかを地に足で検証**）。GPTにできない地上検証をClaudeが担う。
- **GPTを止める要素にしない**：GPT自動化にはGPT-APIのMac miniループ接続が必要。未接続の間はGPTを**非同期の節目レビュー**に留め、毎PRは L1+L2 で回す＝ループは止まらない。

---

## 6. Supervisor構成（監視エージェント）
`tools/autonomy/supervisor.sh`（launchd・拡張予定）。**監視項目**：
- APIコスト（日次）／Maxプラン利用状況／エージェント停止（ハートビート24h）
- CI成功率／PR成功率／Issue処理速度／タスク滞留時間
- 無限ループ検知／同一エラー連続検知／自動PR過多（暴走）
- Mac miniリソース（CPU/メモリ/ストレージ）／ネットワーク異常
- **異常時**：通知（ログ→将来Slack/Discord）＋ `PAUSE` 自動設定 ＋ launchd再起動。

---

## 7. KPI（費用対効果で評価）
「安い」ではなく「費用対効果」。毎日 **AI Studio Daily Report** に自動出力：

| 分類 | 指標 | 目標(初期) |
|---|---|---|
| 稼働 | AI稼働時間 / 停止回数 / 自動復旧回数 | 停止<1回/日 |
| スループット | 完了Issue/日・PR数・レビュー待ち件数 | 完了 8〜15/日 |
| 品質 | **godot-check CI成功率** / マージ後revert率(バグ率) | CI≥90% / revert≤5% |
| 自動化 | 自動マージ率（安全レーン） | ≥80% |
| 速度 | レビュー待ち時間（PR→承認） | 中央値<24h |
| コスト | 日次/月次コスト・**コスト/完了Issue**・**コスト/マージPR** | 監視・上限内 |
| 進捗 | CH01進捗率 / 計画との差異 | 週次で±10%内 |

**費用対効果の主指標＝コスト/マージPR、コスト/完了Issue**。これが下がる構成が最適（＝Sonnet主力＋キャッシュが効く）。

---

## 8. 拡張計画と長期コスト
### フェーズ
GC2026応募(〜2027/3/15・無償公開) → 結果(6/30) → **Steam有償化** → 継続アップデート → **CH02〜CH08** → DLC。

### 稼働強度と年間コスト（C案・目安）
| 期間 | 強度 | 月額目安 | 備考 |
|---|---|---|---|
| 2026/7〜12（CH01完成・高稼働） | 高 | 11〜15万円 | 立ち上げ〜提出品質 |
| 2027/1〜3（polish・応募） | 中 | 8〜12万円 | 演出/素材/応募準備 |
| 2027/4〜（結果待ち〜Steam〜CH02〜） | 中〜継続 | 8〜13万円 | リリース後運用・多章開発 |

- **1年総コスト ≈ 約110〜160万円**（C案）。**2年 ≈ 約230〜320万円**。
- ハードは Mac mini 既所有＋UPS/バックアップ 2〜4万円（一度きり）。電気代・保守は総額の誤差。
- **ROI**：GC2026大賞3億円・Steam売上を狙う投資として、年100万円台は**十分に見合う**（コスト最小化よりスループットと品質を優先すべき理由）。B案（速度）へ一時的にスケールする判断も、応募直前などで有効。

---

## 9. 未確定・要再確認
① 実勢為替 ② Agent SDKクレジット課金の再導入 ③ Sonnet導入価格終了(8/31)後 ④ 採用GPT/Codexモデルの確定単価 ⑤ 自環境の実トークン係数（`count_tokens`実測） ⑥ 各社ToSの最新版での自動化可否。

## 10. 確定事項（v1.0）
- **責務中心設計**：AIを固定せず**責務(Role)を固定**（PM / Architect Review / Implementation / Self Review / Cross AI Review / QA / Human Approval）。担当AIは §2.2 の差替可能マッピングで交換＝**ベンダー非依存**（GPT-6/Claude 6/Codex Next 等へ移行容易）。
- **標準パイプライン**：PM → Issue作成 → Architect Review → Implementation → Self Review → Cross AI Review → CI → Human Review → Merge。Architect Review / Cross Review / Implementation は**責務**であり担当AIは固定しない。
- **AI運用＝C案（対話Max＋無人ループAPI）を採用。**
- **役割**：判断=GPT/人間、実装=Claude(主)/Codex(副)、承認=人間。
- **コスト制御**：Sonnet主力＋キャッシュ＋Batch＋難所Opus＋Supervisor日次上限。
- **評価＝KPI（費用対効果）**、毎日 Daily Report。
- これを今後のAI基盤の標準仕様とする。実値は運用開始後に4章係数とKPI目標を再校正する。
